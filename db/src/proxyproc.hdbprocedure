PROCEDURE "PROXYPRC" (OUT result "SAMPLE2")
LANGUAGE SQLSCRIPT 
SQL SECURITY INVOKER 
AS 
BEGIN 
    --DECLARE lv_idx INT;

    --SELECT max("ID") + 1 INTO lv_idx FROM "SAMPLE2";

    --INSERT INTO "SAMPLE2" VALUES (:lv_idx, 'test' || CAST(:lv_idx AS NVARCHAR(5000)));
    --CALL "ANOTHERSAMPLE"."SAMPLEPROC"(RESULT => ?);

    result = select * from "SAMPLE2";
END